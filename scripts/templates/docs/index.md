# The WebAssembly Linux Interface (WALI)

WALI is a specification that defines how WebAssembly modules can interact with a Linux host through a standardized system call interface.

## Status

This specification is under active development. See the [reference documentation](reference/index.md) for the current implementation status of each system call.
